name: "Hello Actions"
description: "Greet the coder"
author: "alexmarginean16@icloud.com"

inputs:
  MY_NAME:
    description: "Who to greet"
    required: true
    default: "World"

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"
